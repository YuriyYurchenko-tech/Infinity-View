// const express = require("express");
// const fileMiddleware = require("../middlewares/multer");

// const multerRouter = express.Router();

// multerRouter.post("/", fileMiddleware.single("avatar"), (req, res) => {
//   try {
//     if(req.file) {
//       res.json(req.file)
//     }
//   } catch (error) {
//     console.log(error);
//   }
// });

// module.exports = multerRouter;
